
# Deployment Checklist

## Platform

The project is prepared for deployment on Render as a Python FastAPI web service.

## Required Files

The following files must exist before deployment:

```text
.python-version
Procfile
render.yaml
requirements.txt
src/api/app.py
models/inference_bundle.joblib
```
